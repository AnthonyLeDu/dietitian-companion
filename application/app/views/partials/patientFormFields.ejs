<fieldset>
  <label for="last_name">Nom de famille</label>
  <input type="text" name="last_name" id="last_name" placeholder="Dupont" required value=<%= patient.last_name %>>
</fieldset>

<fieldset>
  <label for="first_name">Prénom</label>
  <input type="text" name="first_name" id="first_name" placeholder="Jean" required value=<%= patient.first_name %>>
</fieldset>

<fieldset>
  <label for="gender">Sexe</label>
  <input type="radio" name="gender" id="gender-male" value="male" <% if (patient && patient.gender === 'male') { %> checked <% } %> required>Homme
  <input type="radio" name="gender" id="gender-female" value="female" <% if (patient && patient.gender === 'female') { %> checked <% } %> required>Femme
</fieldset>

<fieldset>
  <label for="birth_date">Date de naissance</label>
  <input type="date" name="birth_date" id="birth_date" max=<%= maxDate %> required value=<%= patient.birth_date %>>
</fieldset>